apiVersion: v1
kind: Service
metadata:
  name: {{ include "ss-ui.fullname" . }}
  labels:
    {{- include "ss-ui.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ss-ui.selectorLabels" . | nindent 4 }}
